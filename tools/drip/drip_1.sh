# coming soon


cat <<EOF

   Selecting Files / Folders 
   When selecting a file or folder:
   * Type ${BOLD}~/${NORMAL} to start autocompleting from the home directory 
   * Type ${BOLD}../${NORMAL} to start autocompleting from the directory above 

EOF



cat <<EOF

   Moving your Cursor
   * Insert characters like moving  
   Try selecting ${MAGENTA}git commit -m${NORMAL}. Fig will automatically insert

EOF